{{#each device in App.devices itemController="deviceItem"}}

	{{#if device.isBinarySwitch}}
		{{view "switchBinary" contentBinding="device"}}
	{{/if}}

	{{#if device.isMultilevelSwitch}}
		{{view "switchMultilevel" contentBinding="device"}}
	{{/if}}

{{/each}}